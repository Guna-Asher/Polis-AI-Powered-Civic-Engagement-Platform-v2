FROM python:3.9-slim

WORKDIR /app

# Copy requirements first for better caching
COPY backend/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy shared models and backend code
COPY shared/ /app/shared/
COPY backend/ /app/

EXPOSE 8000

CMD ["python", "main.py"]